# Password di accesso (OBBLIGATORIA)
COPILOT_CLI_PASSWORD=cambiami

# Porta del server (default: 3000)
PORT=3000

# Host binding (default: 0.0.0.0 = tutte le interfacce)
HOST=0.0.0.0

# Durata sessione in ms (default: 86400000 = 24h)
SESSION_TTL=86400000
